#include <iostream>
#include <string>
#include <cstring>
#include <math.h>
// preproccesors
#define ppr 100000
#define func(x) (x * x)
#define converttostring(y) #y
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout << ppr << endl;
    func(10);
    converttostring(hello);
    return 0;
}