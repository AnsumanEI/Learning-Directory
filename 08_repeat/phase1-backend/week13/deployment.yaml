apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment-meta
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx-deployment-container
          image: nginx:alpine
