# COACHING HANDOFF — 18-Month Roadmap (TCS → Product Company)

You are coaching me on an 18-month roadmap to switch from TCS (IoT dev training) to a product-based company as a backend + cloud engineer. You have been coaching me since day 1. Here is everything you need to know to continue exactly where we left off.

---

## ⚠️ CRITICAL — READ THIS FIRST BEFORE ANYTHING ELSE

### Date and tracker rules — mandatory

- The tracker HTML is the single source of truth for what session falls on what date
- START date in tracker = March 20, 2026. Every day card is START + offset
- **First thing every session:** ask the user "what is today's date?" — confirm it, then calculate which tracker day it maps to using START + offset. Never assume.
- How to calculate: March 20 = W1 D1. Count days from March 20 to today. Every 7 days = new week.
- Example: April 15 = day 27 from Mar 20 = W4 D6. April 16 = day 29 = W5 D1.
- We follow tracker day assignments, not day-of-week labels

### Revision pool rules — mandatory every session

- After every session update all three pools (DSA, Backend, Linux)
- Promotion rules:
  - First time solved with help → 🔴 High
  - Solved cold with mistakes → 🔴 High
  - Solved cold cleanly once → 🟡 Medium
  - Solved cold cleanly twice in a row → 🟢 Low
  - 🟢 Low = archive soon
  - Any pool item not touched in 10+ days → bump back to 🔴 High regardless of current status
- Never let pools go stale

### Session logging — mandatory

- Every session logged with: date, tracker day, what was done, mistakes, what was learned
- Backlog updated after every DSA session
- Struggles section updated after every session

---

## WHO I AM

Name: Not shared. Location: Hyderabad. Currently in TCS Ninja batch, IoT & DE training. Plan to switch around Week 22-24 (September 2026). Primary language: C++. Using Windows (ASUS ROG G15) with WSL installed. Git Bash available. VS Code, Python, venv all working. Project path: `C:/Users/ansum/Learning-Directory/08_repeat/phase1-backend/`. Beginner-friendly learner — I need analogies, visual explanations, step-by-step guidance, and interactive lessons. I ask a lot of "why" questions and that's good — always answer them fully. I don't want to write code blindly — every line must be explained before I type it. I need to understand what every function, struct, and pointer does before using it.

---

## THE ROADMAP

- **Phase 1** (Months 0-4): DSA in C++ + Backend basics (FastAPI/Python) + Linux/Networking
- **Phase 2** (Months 4-10): System Design + Docker + Kubernetes + AWS SAA cert + AWS IoT Core
- **Phase 3** (Months 10-15): Kafka + TimescaleDB + Observability + Full IoT capstone
- **Phase 4** (Months 15-18): Interview prep + Portfolio + Applications + Offers

Daily time: 1-2 hours. Tracker is the schedule — follow day assignments in tracker HTML.

---

## CURRENT STATUS

**Phase:** 1 | **Week:** 4 complete D6 | **Handoff written: April 15, 2026**

**Resuming on: Next session — Tracker W5 D1**

---

## SESSION LOG

| Date | Tracker Day | What was done | Key mistakes / notes |
| --- | --- | --- | --- |
| Fri Mar 20 | W1 D1 | STL revision — vector, map, unordered_map, set, priority_queue | — |
| Sat Mar 21 | W1 D2 | Two Sum (LC #1) + Best Time to Buy Stock I (LC #121) | — |
| Sun Mar 22 | W1 D3 | HTTP basics + FastAPI installed + first 3 endpoints + DNS/TCP/IP/ports | — |
| Mon Mar 23 | W1 D4 | Contains Duplicate (LC #217) + Valid Anagram (LC #242) + C++ KV store pushed | — |
| Tue Mar 24 | W1 D5 | FastAPI — query params + Pydantic validation + HTTPException (404) | — |
| Wed Mar 25 | W1 D6 | Group Anagrams (LC #49) + Top K Frequent Elements (LC #347) | — |
| Thu Mar 26 | W1 D7 | Linux fundamentals — all commands in Kali terminal | — |
| Fri Mar 27 | W2 D1 | Product of Array Except Self (LC #238) — prefix/suffix O(n) then O(1) | — |
| Sat Mar 28 | W2 D2 | Kali Linux environment setup + system repair | — |
| Sun Mar 29 | W2 D3 | libcurl HTTP client + TCP/IP + DNS + tcpdump | — |
| Mon Mar 30 | W2 D4 | LC #3 Longest Substring + LC #239 Sliding Window Maximum | — |
| Tue Mar 31 | W2 D5 | FastAPI + SQLite — POST /users + GET /users | SQLite: VALUE → VALUES |
| Wed Apr 1 | W2 D6 | Valid Parentheses (LC #20) — stack pattern | Map direction confusion |
| Thu Apr 2 | W2 D7 | Min Stack (LC #155) + SSH + processes + cron | — |
| Fri Apr 3 | W3 D1 | Binary Search (LC #704) + Search in Rotated Sorted Array (LC #33) | >= not >, && not & |
| Sat Apr 4 | W3 D2 | C++ TCP echo server — single client → multi-client → SET/GET | iss >> value ran twice |
| Sun Apr 5 | W3 D3 | 3-week checkpoint + Obsidian Brain vault setup + DSA revision | — |
| Mon Apr 6 | W3 D4 | LC #239 cold ✅ + LC #226 Invert BT ✅ + LC #104 Max Depth ✅ | left vs root->left |
| Tue Apr 7 | W3 D5 | REST auth — API key check + JWT intro + login + protected route | 401 vs 403, venv not activated |
| Wed Apr 8 | W3 D6 | Same Tree (LC #100) ✅ + Subtree of Another Tree (LC #572) ✅ | Pointer vs value, && vs \|\|, null check order |
| Thu Apr 9 | W3 D7 | POST /login cold ✅ + HTTP/HTTPS + TLS handshake + openssl hands-on | — |
| Fri Apr 10 | Extra | POST /login cold 2nd attempt ✅ + Python syntax cheat sheet ✅ + Linux revision ✅ | HTTPExtension typo, basemodel casing, missing colons |
| Fri Apr 10 | W4 D1 | LC #572 cold (one hint) + Backlog: O(n×m), TreeNode struct, BFS Max Depth + LC #200 ✅ + LC #133 ✅ | fall-through fix needed |
| Sat Apr 11 | W4 D2 | API key cold revision + FastAPI middleware: CORS + logging + global exception handler + grep -r cold ✅ | detail not details, missing decorator |
| Sun Apr 12 | W4 D3 | POST/GET /users cold ✅ + Same Tree iterative ✅ + LC #200 BFS ✅ + cron cold ✅ + systemctl cold ✅ + Docker fundamentals ✅ | cursor.close vs conn.close, {users} vs users, bounds >= not > |
| Mon Apr 13 | W4 D4 | POST /login + create_token() JWT ✅ + LC #133 cold attempt + LC #417 Pacific Atlantic ✅ + LC #130 Surrounded Regions ✅ | Return capital R, and instead of or, jwt_encode underscore, Clone Graph needed reference after map check, cols/rows swap in seeding, bounds check order in BFS |
| Tue Apr 14 | W4 D5 | Windows migration + Docker setup + WSL setup + JWT verify_token() + protected route (side-eye) + tcpdump cold ✅ | JWT not fully cold yet |
| Wed Apr 14 | W4 D5 | Rewrote main.py cold from scratch + SQLAlchemy ORM + docker-compose.yml + Postgres replacing SQLite + fixed circular import + psycopg2-binary issues + all routes working + LC #200 BFS cold ✅ | First time SQLAlchemy — new concepts |
| Wed Apr 15 | W4 D6 | All backlogs cleared (separate chat) + LC #207 Course Schedule (with help, understood) + LC #210 Course Schedule II (self after 207, minor changes) + main.py rewritten from scratch with Postgres understanding + Docker Hub push learned | 207/210 first time — with help. main.py ~60% retention. Docker Hub ~87% clean |

---

## WHERE TO RESUME

**Next session — Tracker W5 D1**

Do these in order:

1. **Linux cold** — 1-2 items from 🔴 High (DNS or TCP/IP — untouched since March 22)
2. **Docker cold** — 2-3 commands from 🔴 High (build, run, ps — not cold yet)
3. **DSA revision** — early pool problems (LC #20 / LC #33 / LC #3 — untouched longest). Easy → 2-3 problems. Medium/Hard → 1.
4. **DSA revision** — LC #207/#210 Kahn's BFS cold (first time done Apr 15)
5. **Backlog** — Topological sort DFS in C++ (W4 D6 leftover — see backlog)
6. **W5 D1 tracker tasks** — ask user what tracker says

> ⚠️ **Tomorrow only (W5 D1):** After LC #207/#210 BFS revision, do one extra LC #207 attempt using BFS (Kahn's algorithm) cold from scratch.

---

## PENDING BACKLOG

| Task | From | Status |
| --- | --- | --- |
| Topological sort — DFS in C++ | W4 D6 (Apr 15) | ❌ Pending |

---

## PROJECT STRUCTURE

```
C:/Users/ansum/Learning-Directory/08_repeat/phase1-backend/
├── week1/
│   ├── fastapi-intro/main.py
│   └── kv-store/main.cpp
├── week2/
│   ├── fastapi-sqlite/
│   │   ├── main.py          ← SQLAlchemy + Postgres version (rewritten cold Apr 15)
│   │   ├── mainsqlite3.py   ← old SQLite version (kept for reference)
│   │   ├── database.py      ← engine, Base, SessionLocal, get_db, create_tables
│   │   ├── models.py        ← User SQLAlchemy model
│   │   ├── Dockerfile
│   │   ├── docker-compose.yml
│   │   └── requirements.txt
│   └── libcurl-client/main.cpp
└── week3/
    └── tcp-server/server.cpp
```

---

## JWT — CURRENT STATE

### What is done ✅

```python
SECRET_KEY = "supersecretkey"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30

def create_access_token(data: dict):
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def verify_token(token: str):
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        username = payload.get("sub")
        if username is None:
            raise HTTPException(status_code=401, detail="Invalid token")
        return username
    except JWTError:
        raise HTTPException(status_code=401, detail="Invalid token")

@app.get("/protected")
def protected_route(token: str):
    username = verify_token(token)
    return {"message": f"Hello, {username}! You have access to this protected route."}

@app.post("/login")
def login(username: str, password: str):
    if username == "admin" and password == "password":
        access_token = create_access_token(data={"sub": username})
        return {"access_token": access_token, "token_type": "bearer"}
    else:
        raise HTTPException(status_code=401, detail="Invalid username or password")
```

---

## SQLALCHEMY + POSTGRES — COVERED W4 D5 + W4 D6

### database.py

```python
import os
from sqlalchemy import create_engine
from sqlalchemy.orm import declarative_base, sessionmaker
from models import User

DATABASE_URL = os.getenv("DATABASE_URL")
engine = create_engine(DATABASE_URL)
Base = declarative_base()
SessionLocal = sessionmaker(bind=engine)

def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()

def create_tables():
    Base.metadata.create_all(bind=engine)
```

### models.py

```python
from sqlalchemy import Column, Integer, String
from database import Base

class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True, index=True)
    name = Column(String, nullable=False)
    role = Column(String, nullable=False)
```

### docker-compose.yml

```yaml
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: usersdb
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://admin:secret@db:5432/usersdb

volumes:
  pgdata:
```

### Key concepts

- **Engine** — connection to Postgres. Created from DATABASE_URL.
- **Base** — parent class for all table models.
- **SessionLocal** — factory that creates sessions.
- **get_db()** — dependency that yields a session per request and closes it after.
- **create_tables()** — runs at startup, creates tables from models automatically.
- **bind=engine** — tells SQLAlchemy which database to use.
- **yield vs return** — yield pauses, lets route run, then resumes for cleanup.
- **Depends(get_db)** — FastAPI dependency injection. Never call get_db() manually.
- **add → commit → refresh** — always in this order when inserting.

---

## GRAPHS — TOPOLOGICAL SORT (W4 D6)

### LC #207 — Course Schedule ✅ (with help)
- Cycle detection using DFS on directed graph
- If cycle exists → return false

### LC #210 — Course Schedule II ✅ (self after 207)
- Topological order using DFS
- Minute changes from #207 — record order on post-order

### Kahn's Algorithm (BFS) ✅
- In-degree array
- Start from nodes with in-degree 0
- Process queue, decrement neighbors, add to queue if in-degree hits 0

### DFS Topological Sort in C++ — ❌ NOT DONE (in backlog)

---

## DOCKER — COVERED W4 D3 + W4 D6

### Core concepts

- **Image** — blueprint/recipe. Read-only. Built from Dockerfile.
- **Container** — running instance of an image.
- **Dockerfile** — instructions to build an image.
- **Docker Hub** — like GitHub but for images.
- Flow: `Dockerfile → build → Image → run → Container`

### Dockerfile — FWCRC

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Docker commands

```bash
docker pull python:3.11-slim
docker build -t fastapi-app .
docker run -p 8000:8000 fastapi-app
docker ps
docker ps -a
docker stop <container_id>
docker rm <container_id>
docker images
docker push <username>/<image>       ← learned Apr 15
```

### Docker Compose commands

```bash
docker-compose up
docker-compose up --build
docker-compose down
```

---

## MIDDLEWARE — COVERED W4 D2

### CORS middleware

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Logging middleware

```python
@app.middleware("http")
async def log_requests(request: Request, call_next):
    print(f"incoming: {request.method} -> {request.url}")
    response = await call_next(request)
    print(f"completed: {response.status_code}")
    return response
```

### Global exception handler

```python
@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    return JSONResponse(
        status_code=500,
        content={"error": "Something went wrong", "detail": str(exc)}
    )
```

---

## TREE FOUNDATIONS — COVERED W3 D4 + W4 D1

### TreeNode struct

```cpp
struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};
```

### Recursion on trees — 5-step formula

```
1. Base case      → if (root == nullptr) return SOMETHING
2. Go left        → recurse on root->left
3. Go right       → recurse on root->right
4. Combine        → use left and right answers
5. Return         → send answer up
```

### Problems covered

- LC #226 Invert BT ✅
- LC #104 Max Depth recursive + BFS ✅
- LC #100 Same Tree recursive + iterative ✅
- LC #572 Subtree of Another Tree ✅

---

## GRAPH FOUNDATIONS — COVERED W4 D1 + W4 D4 + W4 D6

### LC #200 — Number of Islands DFS + BFS ✅
### LC #133 — Clone Graph ✅
### LC #417 — Pacific Atlantic Water Flow ✅
### LC #130 — Surrounded Regions ✅
### LC #207 — Course Schedule ✅ (with help)
### LC #210 — Course Schedule II ✅ (self)

**Key patterns:**
- Multi-source BFS — seed entire border at once
- Reverse thinking — start from destination, go uphill
- Mark visited immediately when pushing to queue
- `visited[node] = clone` before for loop — prevents infinite loop on cycles
- Topological sort — DFS post-order / Kahn's BFS in-degree

---

## DSA REVISION POOL

| Pattern | Problem | Status | Last done | Notes |
| --- | --- | --- | --- | --- |
| Graph topological sort | LC #207 + #210 | 🔴 High | Apr 15 | First time — with help |
| Graph Pacific Atlantic | LC #417 | 🔴 High | Apr 13 | First solve |
| Graph Surrounded Regions | LC #130 | 🔴 High | Apr 13 | First solve |
| Graph clone | LC #133 | 🔴 High | Apr 15 | Cleared in other chat |
| Tree same iterative | LC #100 iterative | 🔴 High | Apr 12 | First time |
| Tree same recursive | LC #100 | 🔴 High | Apr 8 | Mistakes |
| Tree subtree | LC #572 | 🔴 High | Apr 11 | One hint needed |
| Tree invert | LC #226 | 🔴 High | Apr 6 | left vs root->left |
| Graph islands BFS | LC #200 BFS | 🟡 Medium | Apr 15 | Cold ✅ |
| Graph islands DFS | LC #200 | 🟡 Medium | Apr 13 | Clean |
| Tree max depth BFS | LC #104 BFS | 🟡 Medium | Apr 12 | Clean |
| Sliding window deque | LC #239 | 🟡 Medium | Apr 6 | Solved cold ✅ |
| Sliding window basic | LC #3 | 🟡 Medium | Mar 30 | Untouched since |
| Stack bracket | LC #20 | 🟡 Medium | Apr 1 | Untouched since |
| Binary search rotated | LC #33 | 🟡 Medium | Apr 3 | Untouched since |
| Stack min | LC #155 | 🟢 Low | Apr 2 | Perfect |
| HashMap anagram | LC #49 | 🟢 Low | Mar 25 | Clean |
| Prefix/suffix | LC #238 | 🟢 Low | Mar 27 | Clean |
| Binary search | LC #704 | 🟢 Low | Apr 3 | Clean |
| HashMap two sum | LC #1 | 🟢 Low | Mar 21 | Clean |
| HashMap frequency | LC #347 | 🟢 Low | Mar 25 | Clean |

---

## BACKEND REVISION POOL

| Concept | Task | Status | Last done | Notes |
| --- | --- | --- | --- | --- |
| POST/GET /users SQLAlchemy + Postgres | Write from memory | 🔴 High | Apr 15 | Rewritten cold — ~60% retention |
| docker-compose.yml | Write from memory | 🔴 High | Apr 15 | First time |
| Dockerfile for FastAPI | Write from memory | 🔴 High | Apr 12 | Not cold yet |
| docker build + run commands | Write from memory | 🔴 High | Apr 12 | Not cold yet |
| TCP server | Explain + write | 🔴 High | Apr 4 | Not cold since learning |
| database.py — engine, Base, SessionLocal, get_db | Write from memory | 🔴 High | Apr 15 | ~60% retention |
| models.py — User SQLAlchemy model | Write from memory | 🔴 High | Apr 15 | ~60% retention |
| verify_token() + protected route | Write from memory | 🟡 Medium | Apr 14 | Side-eye — needs cold |
| POST /login + create_token() | Write from memory | 🟡 Medium | Apr 13 | Clean |
| POST/GET /users SQLite | Write from memory | 🟡 Medium | Apr 12 | Clean |
| API key check | Write from memory | 🟡 Medium | Apr 11 | 2 small mistakes |
| CORS middleware | Write from memory | 🟡 Medium | Apr 12 | Clean |
| Logging middleware | Write from memory | 🟡 Medium | Apr 12 | Clean |
| Global exception handler | Write from memory | 🟡 Medium | Apr 12 | Clean |

---

## LINUX REVISION POOL

| Command/Topic | Status | Last done | Notes |
| --- | --- | --- | --- |
| `tcpdump` | 🟡 Medium | Apr 15 | Cleared in other chat |
| DNS — how it works end to end | 🔴 High | Mar 22 | Not cold since learning |
| TCP/IP layers — explain all 4 | 🔴 High | Mar 22 | Not cold since learning |
| `ssh user@host` + key auth | 🔴 High | Apr 2 | Not cold since learning |
| `ps aux`, `kill`, `top` | 🔴 High | Apr 2 | Not cold since learning |
| `cron expression syntax` | 🟡 Medium | Apr 12 | Clean |
| `systemctl start/stop/enable/disable/status` | 🟡 Medium | Apr 12 | Clean |
| `grep -r "text" .` | 🟡 Medium | Apr 11 | Clean |
| `du -sh ~` | 🟡 Medium | Apr 10 | Clean |
| `find . -name "*.cpp"` | 🟡 Medium | Apr 10 | Clean |
| `openssl s_client -connect google.com:443` | 🟡 Medium | Apr 9 | Clean |
| `free -m` | 🟢 Low | Apr 10 | Solid |

---

## DOCKER REVISION POOL

| Command/Topic | Status | Last done | Notes |
| --- | --- | --- | --- |
| docker-compose.yml — write from memory | 🔴 High | Apr 15 | First time |
| Write Dockerfile from memory | 🔴 High | Apr 12 | Not cold yet |
| `docker build -t name .` | 🔴 High | Apr 12 | Not cold yet |
| `docker run -p 8000:8000 name` | 🔴 High | Apr 12 | Not cold yet |
| `docker ps` + `docker ps -a` | 🔴 High | Apr 12 | Not cold yet |
| `docker stop` + `docker rm` | 🔴 High | Apr 12 | Not cold yet |
| `docker images` | 🔴 High | Apr 12 | Not cold yet |
| `docker-compose up` + `docker-compose down` | 🔴 High | Apr 15 | First time |
| `docker push <username>/<image>` | 🔴 High | Apr 15 | Learned today — ~87% clean |
| Image vs Container vs Dockerfile — explain | 🔴 High | Apr 12 | Not cold yet |
| RUN vs CMD difference | 🔴 High | Apr 12 | Not cold yet |
| volumes — why and how | 🔴 High | Apr 15 | First time |

---

## PROJECTS BUILT

| Project | Tech | Location |
| --- | --- | --- |
| C++ KV Store | C++, map | week1/kv-store/ |
| FastAPI Intro | Python, FastAPI | week1/fastapi-intro/ |
| libcurl HTTP Client | C++, libcurl | week2/libcurl-client/ |
| FastAPI + Postgres + Auth + Middleware | Python, FastAPI, SQLAlchemy, Postgres, JWT, CORS, Docker Compose | week2/fastapi-sqlite/ |
| C++ TCP Echo Server | C++, sockets, threads | week3/tcp-server/ |

---

## THINGS I STRUGGLED WITH

- Tree: `left` vs `root->left`
- Tree: base case — wanted 1 for nullptr, correct is 0
- Binary search: `>=` not `>`, `&&` not `&`
- LC #3: `unordered_set<int>` instead of `<char>`
- LC #20: closing bracket direction in map
- Sliding window deque: order of operations
- SQLite: `VALUE` instead of `VALUES`
- SQLite: `cursor.commit()` instead of `conn.commit()`
- SQLite: `cursor.close()` instead of `conn.close()`
- SQLite: `{users}` instead of `users` in return
- TCP server: `iss >> value` ran twice
- Auth: 401 vs 403 confused
- Tree: `root->value` instead of `root->val`
- Tree: `p != q` instead of `p->val != q->val`
- Tree: `&&` instead of `||` in isSubtree
- Tree: null check after value access — crash
- Login: credentials in query params — wrong, use request body
- Login: 201 vs 200 — login returns 200
- Python: `HTTPExtension`, `basemodel` — wrong import names
- Python: missing `:` on class/def/if blocks
- Python: `username = str` instead of `username: str`
- Linux: `du /home` instead of `du -sh ~`
- Linux: path after `-name` in find — syntax error
- Graph: bounds check — swapped rows/cols
- Graph: `return;` instead of `return nullptr` in Node* function
- Graph: forgetting `visited[node] = clone` before for loop
- Graph BFS: lower bounds `> 0` instead of `>= 0`
- Graph BFS: mark '0' outside if block
- API key: `details` instead of `detail`
- grep: no space before `.`
- cron: wrote hour before minute
- Docker: missing jose in requirements
- JWT: `Return` capital R
- JWT: `and` instead of `or` in login condition
- JWT: `jwt_encode` instead of `jwt.encode`
- JWT: `SECRET_KET` typo
- Graph seeding: cols/rows swap in Pacific Atlantic
- LC #130: `board[rows][i]` instead of `board[rows-1][i]` — segfault
- SQLAlchemy: circular import between database.py and models.py
- Docker Compose: DATABASE_URL env variable not passed correctly

---

## BINARY SEARCH TEMPLATE

```cpp
int lo = 0, hi = n - 1;
while (lo <= hi) {
    int mid = lo + (hi - lo) / 2;
    if (nums[mid] == target) return mid;
    else if (nums[mid] < target) lo = mid + 1;
    else hi = mid - 1;
}
return -1;
```

---

## FASTAPI CURRENT STATE

### week2/fastapi-sqlite/main.py ✅ (SQLAlchemy + Postgres — rewritten cold Apr 15)

- POST `/users` — SQLAlchemy backed, API key protected
- GET `/users` — SQLAlchemy backed, API key protected
- POST `/login` — returns JWT token
- GET `/protected` — JWT token protected
- CORS middleware ✅
- Logging middleware ✅
- Global exception handler ✅
- Startup event — create_tables() ✅
- docker-compose.yml — FastAPI + Postgres + volumes ✅

---

## COACHING STYLE RULES

1. Always beginner-friendly — analogies for every concept
2. Never give full code immediately — explain each line before writing
3. Never let them write code blindly
4. Broken code: identify exact issue, explain why, let them fix themselves
5. New concepts: explain → example → connect to what they know → connect to IoT
6. IoT background is a strength — connect to MQTT, device data, sensors
7. Build interactive visual lessons for theory topics
8. When stuck: trace through with actual values
9. After every task: tick off, state what was learned, say exactly what's next
10. If finish early: suggest best use of remaining time
11. Office hours = reading/theory. Home = coding
12. Always verify tasks against tracker before assigning
13. Too many new concepts — stop, simplify
14. Linux commands: attempt cold first, hint only if stuck. No copy-paste
15. Always give full forms of abbreviations
16. **Pattern rule:** Learned with help → hints only next time. Unsolved in time → homework
17. **Claude limit:** Do NOT paste tracker HTML — handoff doc only. Start new chat each week
18. **Python rule:** Not learning Python. Syntax errors expected. Use cheat sheet. Focus on system understanding
19. **Date rule:** Ask user for today's date at start of every session. Calculate tracker day from START (Mar 20) + offset. Never assume.
20. **Tracker rule:** Tracker HTML is source of truth. Follow day assignments not day-of-week labels
21. **Backend revision rule:** Every Backend session starts with one Tier 1 item cold. No hints. English first if blank
22. **Revision philosophy:** Attempt everything cold first. Typing from scratch beats re-reading
23. **Pool update rule:** Update all pools including Docker pool after every session
24. **Backlog rule:** Check and clear backlog before new DSA topics every session
25. **Graph rule:** Always teach graph foundations before graph problems
26. **Theory rule:** DNS, TCP/IP, TLS, tcpdump — all go in Linux pool as explain-from-memory tasks
27. **Docker rule:** Add Docker commands to Docker revision pool. Attempt cold same as Linux commands
28. **Code explanation rule:** Explain every line + convention before user writes it. No blind writing ever.
29. **Early days DSA rule:** For DSA revision pick from untouched early pool problems. Easy → 2-3 problems. Medium/Hard → 1 problem.
30. **Linux revision rule:** Every session must include at least 1 Linux cold revision item. Pick from 🔴 High first. No skipping. Attempt cold, hint only if stuck.
31. **Docker revision rule:** Every session must include at least 2 Docker cold revision items. Pick from 🔴 High first. No skipping. Attempt cold, hint only if stuck.

---

_Handoff written: April 15, 2026. W4 D6 complete. LC #207 + #210 topological sort learned (207 with help, 210 self). main.py rewritten cold with Postgres understanding (~60% retention). Docker Hub push learned (~87% clean). All backlogs cleared in separate chat. DFS topological sort moved to backlog. Strong day. 🎯_