Here's the final handoff with coaching style rules and projects built table added in their correct positions:

---

## COACHING HANDOFF — W5 D7 Complete

---

### Date and tracker rules — mandatory
- Tracker HTML is single source of truth for what session falls on what date
- START date = March 20, 2026. Every day card is START + offset
- **First thing every session:** ask user "what is today's date?" — confirm it, calculate tracker day using START + offset. Never assume.
- March 20 = W1 D1. Count days from March 20 to today. Every 7 days = new week.
- Example: April 21 = W5 D5. April 22 = W5 D6. April 23 = W5 D7.
- Follow tracker day assignments, not day-of-week labels

### Revision pool rules — mandatory every session
- After every session update all four pools (DSA, Backend, Linux, Docker)
- **W5 D7 RESET** — all pools reset to same priority. Old 🔴/🟡/🟢 system retired.
- New tags: `[write]` = cold write from scratch. `[concept]` = explain/understand only, no writing needed
- Rotation going forward: hard → easy → hard → easy
- Any pool item not touched in 10+ days → bump back regardless of current tag
- DSA revision: max 2 per session, only if genuinely quick/easy. Hard = 0 DSA revision, do as main task instead
- Never let pools go stale

### Session logging — mandatory
- Every session logged with: date, tracker day, what was done, mistakes, what was learned
- Backlog updated after every DSA session
- Struggles section updated after every session

### Handoff is reference only from W5 D7 onwards — no code blocks
- All code lives in `08_repeat/phase1-backend/` and `08_repeat/lc-problems/`
- Obsidian vault `10_brain/` has concept explanations
- Handoff only tracks: log, pools, backlog, struggles, where to resume

---

## WHO I AM
Name: Not shared. Location: Hyderabad. Currently in TCS Ninja batch, IoT & DE training. Plan to switch around Week 22-24 (September 2026). Primary language: C++. Using Windows (ASUS ROG G15) with WSL installed. Git Bash available. VS Code, Python, venv all working. Project path: `C:/Users/ansum/Learning-Directory/08_repeat/phase1-backend/`. Beginner-friendly learner — analogies, visual explanations, step-by-step guidance, interactive lessons. I ask a lot of "why" questions — always answer them fully. Don't let me write code blindly — every line must be explained before I type it.

---

## THE ROADMAP
- **Phase 1** (Months 0-4): DSA in C++ + Backend basics (FastAPI/Python) + Linux/Networking
- **Phase 2** (Months 4-10): System Design + Docker + Kubernetes + AWS SAA cert + AWS IoT Core
- **Phase 3** (Months 10-15): Kafka + TimescaleDB + Observability + Full IoT capstone
- **Phase 4** (Months 15-18): Interview prep + Portfolio + Applications + Offers

Daily time: 1-2 hours. Tracker is the schedule — follow day assignments in tracker HTML.

---

## COACHING STYLE RULES

1. Always beginner-friendly — analogies for every concept
2. Never give full code immediately — explain each line before writing
3. Never let them write code blindly
4. Broken code: identify exact issue, explain why, let them fix themselves
5. New concepts: explain → example → connect to what they know → connect to IoT
6. IoT background is a strength — connect to MQTT, device data, sensors
7. Build interactive visual lessons for theory topics
8. When stuck: trace through with actual values
9. After every task: tick off, state what was learned, say exactly what's next
10. If finish early: suggest best use of remaining time
11. Office hours = reading/theory. Home = coding
12. Always verify tasks against tracker before assigning
13. Too many new concepts — stop, simplify
14. Linux commands: attempt cold first, hint only if stuck. No copy-paste
15. Always give full forms of abbreviations
16. **Pattern rule:** Learned with help → hints only next time. Unsolved in time → homework
17. **Claude limit:** Do NOT paste tracker HTML — handoff doc only. Start new chat each week
18. **Python rule:** Not learning Python. Syntax errors expected. Use cheat sheet. Focus on system understanding
19. **Date rule:** Ask user for today's date at start of every session. Calculate tracker day from START (Mar 20) + offset. Never assume.
20. **Tracker rule:** Tracker HTML is source of truth. Follow day assignments not day-of-week labels
21. **Backend revision rule:** Every backend session starts with one item cold. No hints. English first if blank
22. **Revision philosophy:** Attempt everything cold first. Typing from scratch beats re-reading
23. **Pool update rule:** Update all four pools after every session
24. **Backlog rule:** Check and clear backlog before new DSA topics every session
25. **Graph rule:** Always teach graph foundations before graph problems
26. **Theory rule:** DNS, TCP/IP, TLS, tcpdump — all go in Linux pool as explain-from-memory tasks
27. **Docker rule:** Add Docker commands to Docker revision pool. Attempt cold same as Linux commands
28. **Code explanation rule:** Explain every line + convention before user writes it. No blind writing ever.
29. **DSA revision rule:** Easy → max 2 problems. Medium/Hard → 0, do as main task instead
30. **Linux revision rule:** Every session must include at least 1 Linux cold revision item. Pick from highest priority first. No skipping.
31. **Docker revision rule:** Every session must include at least 2 Docker cold revision items. Pick from highest priority first. No skipping.

---

## CURRENT STATUS
**Phase:** 1 | **Week:** 5 complete | **Handoff written: April 23, 2026**
**Resuming on: W6 D1**

---

## SESSION LOG

| Date | Tracker Day | What was done | Key mistakes / notes |
| --- | --- | --- | --- |
| Fri Mar 20 | W1 D1 | STL revision — vector, map, unordered_map, set, priority_queue | — |
| Sat Mar 21 | W1 D2 | Two Sum (LC #1) + Best Time to Buy Stock I (LC #121) | — |
| Sun Mar 22 | W1 D3 | HTTP basics + FastAPI installed + first 3 endpoints + DNS/TCP/IP/ports | — |
| Mon Mar 23 | W1 D4 | Contains Duplicate (LC #217) + Valid Anagram (LC #242) + C++ KV store pushed | — |
| Tue Mar 24 | W1 D5 | FastAPI — query params + Pydantic validation + HTTPException (404) | — |
| Wed Mar 25 | W1 D6 | Group Anagrams (LC #49) + Top K Frequent Elements (LC #347) | — |
| Thu Mar 26 | W1 D7 | Linux fundamentals — all commands in Kali terminal | — |
| Fri Mar 27 | W2 D1 | Product of Array Except Self (LC #238) — prefix/suffix O(n) then O(1) | — |
| Sat Mar 28 | W2 D2 | Kali Linux environment setup + system repair | — |
| Sun Mar 29 | W2 D3 | libcurl HTTP client + TCP/IP + DNS + tcpdump | — |
| Mon Mar 30 | W2 D4 | LC #3 Longest Substring + LC #239 Sliding Window Maximum | — |
| Tue Mar 31 | W2 D5 | FastAPI + SQLite — POST /users + GET /users | SQLite: VALUE → VALUES |
| Wed Apr 1 | W2 D6 | Valid Parentheses (LC #20) — stack pattern | Map direction confusion |
| Thu Apr 2 | W2 D7 | Min Stack (LC #155) + SSH + processes + cron | — |
| Fri Apr 3 | W3 D1 | Binary Search (LC #704) + Search in Rotated Sorted Array (LC #33) | >= not >, && not & |
| Sat Apr 4 | W3 D2 | C++ TCP echo server — single client → multi-client → SET/GET | iss >> value ran twice |
| Sun Apr 5 | W3 D3 | 3-week checkpoint + Obsidian Brain vault setup + DSA revision | — |
| Mon Apr 6 | W3 D4 | LC #239 cold ✅ + LC #226 Invert BT ✅ + LC #104 Max Depth ✅ | left vs root->left |
| Tue Apr 7 | W3 D5 | REST auth — API key check + JWT intro + login + protected route | 401 vs 403, venv not activated |
| Wed Apr 8 | W3 D6 | Same Tree (LC #100) ✅ + Subtree of Another Tree (LC #572) ✅ | Pointer vs value, && vs \|\|, null check order |
| Thu Apr 9 | W3 D7 | POST /login cold ✅ + HTTP/HTTPS + TLS handshake + openssl hands-on | — |
| Fri Apr 10 | Extra | POST /login cold 2nd attempt ✅ + Python syntax cheat sheet ✅ + Linux revision ✅ | HTTPExtension typo, basemodel casing, missing colons |
| Fri Apr 10 | W4 D1 | LC #572 cold (one hint) + Backlog: O(n×m), TreeNode struct, BFS Max Depth + LC #200 ✅ + LC #133 ✅ | fall-through fix needed |
| Sat Apr 11 | W4 D2 | API key cold revision + FastAPI middleware: CORS + logging + global exception handler + grep -r cold ✅ | detail not details, missing decorator |
| Sun Apr 12 | W4 D3 | POST/GET /users cold ✅ + Same Tree iterative ✅ + LC #200 BFS ✅ + cron cold ✅ + systemctl cold ✅ + Docker fundamentals ✅ | cursor.close vs conn.close, {users} vs users, bounds >= not > |
| Mon Apr 13 | W4 D4 | POST /login + create_token() JWT ✅ + LC #133 cold attempt + LC #417 Pacific Atlantic ✅ + LC #130 Surrounded Regions ✅ | Return capital R, and instead of or, jwt_encode underscore, Clone Graph needed reference after map check, cols/rows swap in seeding, bounds check order in BFS |
| Tue Apr 14 | W4 D5 | Windows migration + Docker setup + WSL setup + JWT verify_token() + protected route (side-eye) + tcpdump cold ✅ | JWT not fully cold yet |
| Wed Apr 14 | W4 D5 | Rewrote main.py cold from scratch + SQLAlchemy ORM + docker-compose.yml + Postgres replacing SQLite + fixed circular import + psycopg2-binary issues + all routes working + LC #200 BFS cold ✅ | First time SQLAlchemy — new concepts |
| Wed Apr 15 | W4 D6 | All backlogs cleared + LC #207 Course Schedule (with help) + LC #210 Course Schedule II (self) + main.py rewritten cold with Postgres + Docker Hub push learned | 207/210 first time — with help. main.py ~60% retention. Docker Hub ~87% clean |
| Thu Apr 16 | W4 D7 | DNS cold ✅ + docker build/run cold ✅ + LC #20 cold ✅ + database.py cold (~30% retention) ✅ + Dockerized TCP server ✅ + LC #33 cold ✅ + LC #207 fully cold ✅ + LC #210 cold ✅ + DFS topo sort intro (stopped) | docker tag syntax missed, database.py ~30% cold retention |
| Fri Apr 17 | W5 D1 | TCP/IP layers cold ✅ + docker tag/ps/stop/rm cold ✅ + models.py cold ✅ + LC #3 cold ✅ + LC #572 cold ✅ + DFS topo sort built from scratch ✅ + LC #70 Climbing Stairs — all 3 versions ✅ | TCP/IP layer names needed, docker ps swapped, models.py 3 small fixes, LC #572 missing return, LC #70 memo pass by value + infinite loop |
| Sat Apr 18 | W5 D2 | SSH key auth taught + ps aux/kill revised + RUN vs CMD taught + verify_token() cold ✅ + protected route cold (decorator + syntax fixes) + LC #198 House Robber DP (memo + tabulation) ✅ + LC #226 Invert BT cold (missing return) + Full JWT auth flow built: /register + /login DB-backed + /protected Bearer token + bcrypt password hashing + passlib + OAuth2PasswordBearer + curl mastered | verify_token return was set not username, protected route used POST not GET + missing colons, House Robber missing returns in dp() and rob(), Invert BT missing return root, bcrypt==4.0.1 pin needed, schemes needs list not string |
| Sun Apr 19 | W5 D3 | ps aux/kill cold ✅ (order fixed) + Docker volumes taught ✅ + Full backend cold rebuild: database.py + models.py + main.py with /register /login /protected ✅ + LC #226 cold ✅ + LC #100 recursive + iterative cold ✅ + LC #1143 LCS — recursion derived independently + memo + tabulation + O(n) space ✅ + LC #72 Edit Distance memo ✅ | database.py: create_engine("string") not variable, tabulation == not =, prev=cur inside inner loop not outside, Edit Distance base case j+1/i+1 not 0, missing .first() on register query, sessionmaker wrong import |
| Mon Apr 20 | W5 D4 | Dockerfile for FastAPI cold (mistakes) + docker-compose.yml skeleton remembered + LC #133 Clone Graph cold ✅ + Full CRUD API for /devices resource with JWT auth — all 5 routes + curl tested ✅ | Dockerfile: wrong base image (alpine vs python:3.11-slim), wrong order (copy before install), typo apline. docker-compose: image/build/env details missing. LC #133: missing return clone. POST /devices: positional args instead of keyword args. GET+DELETE by id: ID from Header instead of path param, string "ID" instead of variable, db.refresh after delete, 404 used 401 status code. SQLAlchemy filter: & instead of comma, missing .first() again |
| Tue Apr 21 | W5 D5 | Dockerfile cold ✅ (one missing RUN) + docker-compose.yml cold (structure good, details missing) + LC #139 Word Break — recursion ✅ + memoisation ✅ + bottom-up not completed (tired, outside all day) | Dockerfile: missing RUN before pip install. docker-compose: environment typo, ports not port, build syntax, depends_on underscore, volumes path missing, Postgres env vars missing, DATABASE_URL value missing. LC #139 bottom-up: stopped — fatigue |
| Wed Apr 22 | W5 D6 | nginx reverse proxy concept learned + nginx.conf written from scratch + .env file created + os.getenv() + load_dotenv() added to main.py + healthcheck added to docker-compose + full stack running on port 80 + all routes tested through nginx ✅ | depends_on list vs map syntax when adding condition. .env credential mismatch. healthcheck pg_isready FATAL on ansuman966 — cosmetic. curl header underscore vs hyphen — FastAPI requires x-api-key not x_api_key |
| Thu Apr 23 | W5 D7 | Month 1 checkpoint — TCP server concept revision ✅ + nginx cold write ✅ + 26 LC problems listed and categorised ✅ + devices API demoed ✅ + Docker stack confirmed working ✅ + README updated ✅ | — |

---

## WHERE TO RESUME
**Next session — W6 D1**

Check tracker for W6 D1 assignment first. Then do these revisions before tasks:

1. **Backend** — nginx + nginx.conf [write] (first clean cold yesterday — needs second cold)
2. **Docker** — Dockerfile for FastAPI [write] (one missing RUN last time)
3. **Docker** — docker-compose FastAPI+Postgres+nginx [write] (details still missing)
4. **Linux** — ssh key auth [concept] (taught Apr 18, not cold yet)
5. **DSA** — LC #417 Pacific Atlantic [write] (10+ days untouched — hard, do as main task not revision)

**Pending backlog:**
- LC #139 Word Break — bottom-up DP (pushed from W5 D5)
- LC #72 Edit Distance — tabulation + O(n) space (pushed from W5 D3)

---

## PENDING BACKLOG

- LC #139 Word Break — bottom-up DP (pushed from W5 D5)
- LC #72 Edit Distance — tabulation + O(n) space (pushed from W5 D3)

---

## PROJECTS BUILT

| Project | Tech | Location |
| --- | --- | --- |
| C++ KV Store | C++, map | week1/kv-store/ |
| FastAPI Intro | Python, FastAPI | week1/fastapi-intro/ |
| libcurl HTTP Client | C++, libcurl | week2/libcurl-client/ |
| FastAPI + Postgres + JWT + bcrypt + nginx | Python, FastAPI, SQLAlchemy, Postgres, JWT, Docker, nginx | week2/fastapi-sqlite/ |
| C++ TCP Echo Server (Dockerized) | C++, sockets, threads, Docker, Alpine | week3/tcp-server/ |

---

## NGINX — COVERED W5 D6

### What is nginx
nginx sits in front of FastAPI as a reverse proxy. User never talks to FastAPI directly.
```
User → port 80 → nginx → port 8000 → FastAPI
```

### nginx.conf — see `week2/fastapi-sqlite/nginx/nginx.conf`
Key concepts: `listen 80`, `location /`, `proxy_pass http://web:8000` — `web` is docker-compose service name, Docker DNS resolves it automatically.

### File location inside container
`/etc/nginx/conf.d/default.conf`

### docker-compose volume mount
`./nginx/nginx.conf:/etc/nginx/conf.d/default.conf`

---

## .ENV + OS.GETENV — COVERED W5 D6

### .env file — see `week2/fastapi-sqlite/.env` (never commit — in .gitignore)
Variables: POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, DATABASE_URL, SECRET_KEY, ALGORITHM, ACCESS_TOKEN_EXPIRE_MINUTES, API_KEY

### Key concepts
- Inside Docker — docker-compose injects `.env` automatically, `os.getenv()` picks it up
- Local dev — `load_dotenv()` loads `.env` into environment first
- Credentials must be consistent — POSTGRES_USER/PASSWORD/DB must match DATABASE_URL exactly

---

## DOCKER-COMPOSE WITH NGINX + HEALTHCHECK — COVERED W5 D6

### Full docker-compose — see `week2/fastapi-sqlite/docker-compose.yml`

### Key concepts
- `pg_isready -U` — checks if Postgres is accepting connections
- `interval: 5s` — check every 5 seconds
- `retries: 5` — try 5 times before marking unhealthy
- `condition: service_healthy` — web waits until db passes healthcheck
- `depends_on` with condition uses map syntax not list syntax

---

## DP — WORD BREAK (LC #139) — COVERED W5 D5

### Recursion + Memoisation — see `08_repeat/lc-problems/lc-139-word-break.cpp`

### Bottom-up — TO DO next session
- `dp[i]` = can `s[0..i-1]` be segmented
- `dp[0] = true`
- For each `i`, check all `j < i` — if `dp[j]` true and `s.substr(j, i-j)` in dict → `dp[i] = true`
- Return `dp[n]`

---

## CRUD DEVICES API — COVERED W5 D4

### All 5 routes — see `week2/fastapi-sqlite/main.py`
Key mistakes fixed: keyword args not positional, path param not Header, no db.refresh after delete, 404 not 401, comma not & in filter, always need .first()

---

## DP — LCS (LC #1143) — COVERED W5 D3

### All 3 versions — see `08_repeat/lc-problems/lc-1173-longest-sub-sequence.cpp`

### Recurrence
- Match: `dp[i][j] = 1 + dp[i-1][j-1]`
- No match: `dp[i][j] = max(dp[i-1][j], dp[i][j-1])`

---

## DP — EDIT DISTANCE (LC #72) — COVERED W5 D3

### Memoisation — see `08_repeat/lc-problems/lc-72-edit-distance.cpp`

### Recurrence
- Match: `dp[i][j] = dp[i-1][j-1]`
- No match: `dp[i][j] = 1 + min(delete, insert, replace)`

### Base cases
- `i < 0` → `j + 1`
- `j < 0` → `i + 1`

### Tabulation + O(n) space — TO DO (backlog)

---

## DP — HOUSE ROBBER (LC #198) — COVERED W5 D2

### Full solution — see `08_repeat/lc-problems/lc-198-house-robber.cpp`

### Recurrence
`dp[i] = max(nums[i] + dp[i-2], dp[i-1])`

---

## JWT AUTH — FULL FLOW — COVERED W5 D2

### Full implementation — see `week2/fastapi-sqlite/main.py`

### Key concepts
- **CryptContext** — hashing manager. `hash()` to save, `verify()` to check
- **OAuth2PasswordBearer** — reads Bearer token from Authorization header automatically
- **hash is one-way** — never decrypt, always rehash and compare
- **bcrypt==4.0.1** — pin this version, newer bcrypt breaks passlib
- **schemes needs list** — `schemes=["bcrypt"]` not `schemes="bcrypt"`

---

## DP — CLIMBING STAIRS (LC #70) — COVERED W5 D1

### All 3 versions — see `08_repeat/lc-problems/lc-70-climbing-stairs.cpp`

### Key insight
`climb(n) = climb(n-1) + climb(n-2)` — Fibonacci in disguise

---

## DFS TOPOLOGICAL SORT — COVERED W5 D1

### Full solution — see `08_repeat/excercise/lc-207-course-schedule-dfs.cpp`

### Key concepts
- Three states: 0 = unvisited, 1 = visiting, 2 = visited
- Visiting node seen again → cycle detected
- Push node to result AFTER all neighbors processed (post-order)

---

## SQLALCHEMY + POSTGRES — CURRENT STATE

### All files — see `week2/fastapi-sqlite/`
- `database.py` — engine, Base, SessionLocal, get_db, create_tables
- `models.py` — User, UserAuth, Devices
- `docker-compose.yml` — db + web + nginx + healthcheck + volumes

### Key concepts
- `create_engine(DATABASE_URL)` — variable not string
- `SessionLocal = sessionmaker(bind=engine)` — lives in sqlalchemy.orm
- `add → commit → refresh` — always this order when inserting
- `Depends(get_db)` — never call get_db() manually
- Always `.first()` on queries — query object without it is always truthy

---

## FASTAPI CURRENT STATE

### week2/fastapi-sqlite/main.py ✅
- POST `/register` — bcrypt hashed password, DB backed ✅
- POST `/login` — DB lookup + bcrypt verify + JWT token ✅
- GET `/protected` — Bearer token via OAuth2PasswordBearer + API key ✅
- POST `/users` — SQLAlchemy backed, API key protected ✅
- GET `/users` — SQLAlchemy backed, API key protected ✅
- GET `/devices` — JWT auth protected ✅
- POST `/devices` — JWT auth + duplicate check ✅
- GET `/devices/{id}` — JWT auth + 404 handling ✅
- PUT `/devices/{id}` — JWT auth + 404 handling ✅
- DELETE `/devices/{id}` — JWT auth + 404 handling ✅
- CORS middleware ✅
- Logging middleware ✅
- Global exception handler ✅
- Startup event — create_tables() ✅
- docker-compose — FastAPI + Postgres + nginx + healthcheck + .env ✅
- nginx reverse proxy on port 80 ✅
- .env + os.getenv() + load_dotenv() ✅

---

## TREE FOUNDATIONS

### TreeNode struct — see `08_repeat/lc-problems/`

### Recursion on trees — 5-step formula
1. Base case → if (root == nullptr) return SOMETHING
2. Go left → recurse on root->left
3. Go right → recurse on root->right
4. Combine → use left and right answers
5. Return → send answer up

### Problems covered
- LC #226 Invert BT ✅
- LC #104 Max Depth recursive + BFS ✅
- LC #100 Same Tree recursive + iterative ✅
- LC #572 Subtree of Another Tree ✅

---

## GRAPH FOUNDATIONS

### Problems covered
- LC #200 Number of Islands DFS + BFS ✅
- LC #133 Clone Graph ✅
- LC #417 Pacific Atlantic Water Flow ✅
- LC #130 Surrounded Regions ✅
- LC #207 Course Schedule — Kahn's BFS ✅ + DFS topo sort ✅
- LC #210 Course Schedule II — Kahn's BFS ✅

### Key patterns
- Multi-source BFS — seed entire border at once
- Reverse thinking — start from destination, go uphill
- Mark visited immediately when pushing to queue
- `visited[node] = clone` before for loop — prevents infinite loop on cycles
- Topological sort — DFS post-order / Kahn's BFS in-degree

---

## DOCKER — CURRENT STATE

### Core concepts
- **Image** — blueprint. Read-only. Built from Dockerfile
- **Container** — running instance of an image
- **Dockerfile** — instructions to build an image
- **Docker Hub** — like GitHub but for images
- **Alpine** — tiny 5MB Linux base image. `apk` is its package manager
- **RUN** — build time. Installs things into image permanently
- **CMD** — run time. Executes when container starts. Only last CMD wins
- **volumes** — map host folder to container folder. Data survives container death
- Flow: `Dockerfile → build → Image → run → Container`

### Dockerfile for FastAPI — see `week2/fastapi-sqlite/Dockerfile`
Order: FROM → WORKDIR → COPY requirements.txt → RUN pip install → COPY . . → CMD

### Docker commands
```bash
docker build -t fastapi-app .
docker run -p 8000:8000 fastapi-app
docker ps / docker ps -a
docker stop <id> / docker rm <id>
docker images
docker tag <local> <username>/<repo>:<version>
docker push <username>/<repo>:<version>
docker-compose up / docker-compose up --build / docker-compose down
docker-compose down -v   # removes volumes — wipes database
```

---

## BINARY SEARCH TEMPLATE

```cpp
int lo = 0, hi = n - 1;
while (lo <= hi) {
    int mid = lo + (hi - lo) / 2;
    if (nums[mid] == target) return mid;
    else if (nums[mid] < target) lo = mid + 1;
    else hi = mid - 1;
}
return -1;
```

---

## PROJECT STRUCTURE

```
C:/Users/ansum/Learning-Directory/08_repeat/phase1-backend/
├── week1/
│   ├── fastapi-intro/main.py
│   └── kv-store/main.cpp
├── week2/
│   ├── fastapi-sqlite/
│   │   ├── main.py
│   │   ├── database.py
│   │   ├── models.py
│   │   ├── Dockerfile
│   │   ├── docker-compose.yml
│   │   ├── requirements.txt
│   │   ├── .env              ← never commit
│   │   └── nginx/
│   │       └── nginx.conf
│   └── libcurl-client/main.cpp
└── week3/
    └── tcp-server/
        ├── server.cpp
        ├── Dockerfile
        └── docker-compose.yml
```

---

## DSA REVISION POOL

All items reset W5 D7. Tags: [write] / [concept]. Rotation: hard → easy → hard → easy.

| Pattern | Problem | Tag | Last done | Notes |
| --- | --- | --- | --- | --- |
| Graph Pacific Atlantic | LC #417 | [write] | Apr 13 | Not cold since first solve |
| Graph Surrounded Regions | LC #130 | [write] | Apr 13 | Not cold since first solve |
| DP LCS | LC #1143 | [write] | Apr 19 | All 3 versions — first solve |
| DP Edit Distance | LC #72 | [write] | Apr 19 | Memo only — tabulation + O(n) space pending |
| DP Word Break | LC #139 | [write] | Apr 21 | Recursion + memo done — bottom-up pending |
| DP Coin Change | LC #322 | [write] | Apr 23 | First solve — all 3 versions |
| Graph clone | LC #133 | [write] | Apr 20 | Missing return fixed |
| Graph topo sort DFS | LC #207 DFS | [write] | Apr 17 | Built from scratch ✅ |
| Graph topological sort BFS | LC #207 + #210 | [write] | Apr 16 | Fully cold ✅ |
| Graph islands | LC #200 DFS+BFS | [write] | Apr 15 | Both clean |
| Tree invert | LC #226 | [write] | Apr 19 | Clean cold ✅ |
| Tree same | LC #100 recursive+iterative | [write] | Apr 19 | Both clean ✅ |
| Tree subtree | LC #572 | [write] | Apr 17 | Clean |
| Tree max depth | LC #104 recursive+BFS | [write] | Apr 6 | Clean |
| DP House Robber | LC #198 | [write] | Apr 18 | Memo + tabulation |
| DP Climbing Stairs | LC #70 | [write] | Apr 17 | All 3 versions |
| Sliding window deque | LC #239 | [write] | Apr 6 | Solved cold ✅ |
| Binary search rotated | LC #33 | [write] | Apr 16 | Cold ✅ |
| Stack bracket | LC #20 | [write] | Apr 16 | Cold ✅ |
| Sliding window basic | LC #3 | [write] | Apr 17 | Cold ✅ |
| Stack min | LC #155 | [write] | Apr 2 | Clean |
| HashMap anagram | LC #49 | [write] | Mar 25 | Clean |
| Prefix/suffix | LC #238 | [write] | Mar 27 | Clean |
| Binary search | LC #704 | [write] | Apr 3 | Clean |
| HashMap two sum | LC #1 | [write] | Mar 21 | Clean |
| HashMap frequency | LC #347 | [write] | Mar 25 | Clean |

---

## BACKEND REVISION POOL

| Concept | Tag | Last done | Notes |
| --- | --- | --- | --- |
| nginx + nginx.conf | [write] | Apr 23 | Cold clean ✅ — needs second cold |
| docker-compose FastAPI+Postgres+nginx | [write] | Apr 22 | Structure clean, details need drilling |
| Dockerfile for FastAPI | [write] | Apr 21 | One missing RUN |
| .env + os.getenv() + load_dotenv() | [write] | Apr 22 | Clean first time |
| Full CRUD /devices — all 5 routes | [write] | Apr 20 | Path params, keyword args solid |
| /register + /login DB-backed | [write] | Apr 19 | 2 small bugs |
| verify_token() + protected route | [write] | Apr 19 | Clean |
| database.py | [write] | Apr 19 | One bug: string vs variable |
| models.py — User + UserAuth + Devices | [write] | Apr 20 | Clean |
| hash_password + verify_hash | [write] | Apr 18 | Clean |
| OAuth2PasswordBearer + Depends | [write] | Apr 18 | Clean |
| POST /login + create_token() JWT | [write] | Apr 13 | Clean |
| POST/GET /users SQLAlchemy | [write] | Apr 15 | ~60% retention |
| API key middleware | [write] | Apr 11 | 2 small mistakes |
| CORS + logging + exception handler | [write] | Apr 12 | Clean |
| TCP server — socket/bind/listen/accept | [concept] | Apr 23 | Can explain all steps, blank on cold write |

---

## LINUX REVISION POOL

| Command/Topic | Tag | Last done | Notes |
| --- | --- | --- | --- |
| `ps aux \| grep` + `kill -9` | [write] | Apr 19 | Order fixed |
| `ssh user@host` + key auth | [concept] | Apr 18 | Taught — not cold yet |
| TCP/IP layers — explain all 4 | [concept] | Apr 17 | Names needed, concepts solid |
| DNS end to end | [concept] | Apr 16 | Clean |
| `tcpdump` | [write] | Apr 15 | Clean |
| `cron expression syntax` | [write] | Apr 12 | Clean |
| `systemctl start/stop/enable/status` | [write] | Apr 12 | Clean |
| `grep -r "text" .` | [write] | Apr 11 | Clean |
| `du -sh ~` | [write] | Apr 10 | Clean |
| `find . -name "*.cpp"` | [write] | Apr 10 | Clean |
| `openssl s_client -connect google.com:443` | [write] | Apr 9 | Clean |
| `free -m` | [write] | Apr 10 | Solid |

---

## DOCKER REVISION POOL

| Command/Topic | Tag | Last done | Notes |
| --- | --- | --- | --- |
| nginx + nginx.conf | [write] | Apr 23 | Cold clean ✅ — promote if clean again next session |
| docker-compose FastAPI+Postgres+nginx | [write] | Apr 22 | Healthcheck + condition syntax solid |
| Dockerfile for FastAPI | [write] | Apr 21 | One missing RUN |
| Docker volumes | [concept] | Apr 19 | Clean |
| RUN vs CMD | [concept] | Apr 18 | Clean |
| `docker ps` + `docker ps -a` | [write] | Apr 17 | Clean |
| `docker stop` + `docker rm` | [write] | Apr 17 | Clean |
| `docker tag` | [write] | Apr 17 | Clean |
| `docker images` | [write] | Apr 18 | Clean |
| `docker build -t name .` | [write] | Apr 16 | Clean |
| `docker run -p 8000:8000 name` | [write] | Apr 16 | Clean |
| `docker push <username>/<image>` | [write] | Apr 16 | Clean |
| `docker-compose up --build` | [write] | Apr 16 | Clean |
| Dockerfile for C++ alpine | [write] | Apr 16 | Clean |
| `docker-compose up` + `docker-compose down` | [write] | Apr 15 | Clean |
| Image vs Container vs Dockerfile | [concept] | Apr 17 | Solid |

---

## THINGS I STRUGGLED WITH

- Tree: `left` vs `root->left`
- Tree: base case — wanted 1 for nullptr, correct is 0
- Tree: `root->value` instead of `root->val`
- Tree: `p != q` instead of `p->val != q->val`
- Tree: `&&` instead of `||` in isSubtree
- Tree: null check after value access — crash
- Tree: missing `return root` in invert BT
- Binary search: `>=` not `>`, `&&` not `&`
- LC #3: `unordered_set<int>` instead of `<char>`
- LC #20: closing bracket direction in map
- LC #33: `<=` boundary — self caught ✅
- LC #70: memo pass by value — changes lost
- LC #70: `i < n` instead of `i <= n` + missing increment in tabulation
- LC #133: missing `visited[node] = clone` before for loop
- LC #133: missing return clone
- LC #139: bottom-up couldn't process after long day outside — fatigue not logic gap
- LC #198: missing `return current` in dp() + missing `return` in rob()
- LC #572: called isSametree without returning result
- Graph: bounds check — swapped rows/cols
- Graph BFS: lower bounds `> 0` instead of `>= 0`
- Graph BFS: mark '0' outside if block
- Graph: `return;` instead of `return nullptr` in Node* function
- Graph seeding: cols/rows swap in Pacific Atlantic
- LC #130: `board[rows][i]` instead of `board[rows-1][i]` — segfault
- SQLite: `VALUE` instead of `VALUES`
- SQLite: `cursor.commit()` / `cursor.close()` instead of conn
- SQLite: `{users}` instead of `users` in return
- SQLAlchemy: circular import between database.py and models.py
- SQLAlchemy: missing `.first()` on queries — multiple times
- SQLAlchemy: positional args instead of keyword args in model creation
- SQLAlchemy: `&` instead of comma for multiple filter conditions
- SQLAlchemy: `db.refresh` after delete — object no longer exists
- SQLAlchemy: used 401 status code for 404 not found
- TCP server: `iss >> value` ran twice
- Auth: 401 vs 403 confused
- Login: credentials in query params — wrong, use request body
- Python: `HTTPExtension`, `basemodel` — wrong import names
- Python: missing `:` on class/def/if blocks
- Python: `Return` capital R
- Python: `and` instead of `or` in login condition
- Python: `jwt_encode` instead of `jwt.encode`
- Python: `SECRET_KET` typo
- passlib: `schemes="bcrypt"` needs to be list `schemes=["bcrypt"]`
- bcrypt: version conflict — pin `bcrypt==4.0.1`
- verify_token: returned `{f"..."}` set instead of username
- protected route: used `@app.post` instead of `@app.get`
- GET/DELETE by id: used `Header()` instead of path parameter
- GET/DELETE by id: `"ID"` string instead of variable
- database.py: `create_engine("DATABASE_URL")` — string not variable
- tabulation: `==` instead of `=` — comparison not assignment
- tabulation O(n): `prev = cur` inside inner loop — must be outside
- Edit Distance: base case is `j+1` / `i+1` not `0`
- register query: missing `.first()` — query object always truthy without it
- sessionmaker: wrong import path
- Dockerfile: `alpine` instead of `python:3.11-slim` — no Python in alpine
- Dockerfile: `apline` typo — build fails
- Dockerfile: COPY all before install — breaks layer caching
- Dockerfile: missing RUN before pip install
- docker-compose: `environment` typo, `ports` vs `port`, `build: .` syntax
- docker-compose: `depends_on` underscore, volumes path missing
- docker-compose: list vs map syntax when adding `condition`
- docker tag: missed `tag` keyword and username/repo:version format
- database.py: ~30% cold retention
- .env: credential mismatch — POSTGRES_USER/PASSWORD/DB must match DATABASE_URL
- nginx: `proxy_pass` syntax, semicolons required
- curl: FastAPI requires `x-api-key` with hyphen not underscore
- curl: space before colon in header breaks nginx with 400
- grep: no space before `.`
- cron: wrote hour before minute
- Linux: `du /home` instead of `du -sh ~`
- Linux: path after `-name` in find — syntax error
- API key: `details` instead of `detail`
- DFS topo sort: `else return true` exits loop early — fixed
- healthcheck: pg_isready FATAL on username — cosmetic only, app unaffected

---

*Handoff written: April 23, 2026. W5 D7 complete. Month 1 checkpoint done — 26 LC problems categorised, full dockerised FastAPI stack with nginx + healthcheck + .env working, README updated. All pools reset with [write]/[concept] tags. No code blocks from here — code lives in files. Backlog: LC #139 bottom-up, LC #72 tabulation. W6 D1 next. 🎯*