#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // records are deleted as we proceed in recursion
    // the memory taken is in degree of n
    // n is the degree of polynomial
    return 0;
}