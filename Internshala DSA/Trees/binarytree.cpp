#include <iostream>
#include <bits/stdc++.h>
#include <math.h>
#include <strings.h>
#include <string>
#include <cstring>
using namespace std;
int main()
{
    // maximum two child nodes
    // two types of representation
    // 1.array type left node = 2n , right = 2n + 1
    // 2.linkedlist type [lptr , data , rptr]
    // height or depth of bt = longest path from parent to leaf node
    // height = leaf + 1;

    // pre order traversal -> root -> left - > right
    // in order  left -> root -> right
    // post order left -> right -> root
    return 0;
}